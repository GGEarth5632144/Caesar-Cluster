# Dev-only image: runs the Vite dev server with hot reload.
# Source is bind-mounted in from docker-compose, so COPY here only seeds
# node_modules into the image layer/cache.
FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
