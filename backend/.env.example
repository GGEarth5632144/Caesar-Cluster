PORT=8080
DB_URL=postgres://postgres:password@localhost:5433/cloud_cluster?sslmode=disable
JWT_SECRET=dev-secret
FRONTEND_ORIGIN=http://localhost:5173
PROVISIONER=mock
# ต้องกำหนดเมื่อ PROVISIONER=proxmox เท่านั้น
PROXMOX_URL=https://10.x.x.x:8006/api2/json
PROXMOX_TOKEN=PVEAPIToken=user@realm!tokenid=uuid